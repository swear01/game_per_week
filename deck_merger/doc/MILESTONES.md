# deck_merger — 實作里程碑（粗粒度）

與 [企劃書](./企劃書.md) 對齊；細項實作時再拆 issue 即可。

**狀態說明（2026-04）：** 已定稿規則之終端原型已完成；企劃 §5.2【6】～【9】之 `use` 已實裝（取得途徑仍待版本擴充）。開局預設為**遺物二選一**（與里程碑相同之未持有池），選後才抽牌；`--no-default-relics` 略過開局選遺物。啟動方式見根目錄 [README.md](../README.md)。

## M0 — 專案骨架

- [x] Python 套件（`src/` + setuptools `package-dir`）、`__main__`、`pyproject.toml`、`pytest` 可執行
- [x] 人類模式迴圈 + `--agent` + `--seed` 參數

## M1 — 核心狀態與規則迴圈

- [x] `Card` / `GameState`、抽牌堆／棄牌／手牌（三區）、洗牌與 Exhaust
- [x] 合成：產物 + 素材**全進棄牌**；墊腳石／【1+1+1】已接（差 1 二合一僅墊腳石等來源）
- [x] 回合開始抽牌（含 One More 加成）、回合結束清場、能量重置／電容器邏輯
- [x] `max_value_ever_seen`、勝利為**打出牌面 10**（`use 10`；第 **15** 回合末仍未勝則敗）、敗北 **第 3/6/10 回合末**階段檢核（里程碑 4／6／8）

## M2 — AI 與動作介面

- [x] **`legal_actions`**：列舉合法步、JSON 鍵排序**去重**
- [x] JSON 行協定：精簡 `state`（觀測）／錯誤；`all_actions` 查詢回傳 `legal_actions`；啟動時 `bootstrap` 一行（無 `legal_actions`）

## M3 — 卡牌效果

- [x] 牌面【1】～【5】之 `use` 效果（依牌面數字綁定，含初始空白牌）、耗能檢查、能量不為負、payload 與 Exhaust
- [x] 【6】～【9】之 `use` 效果（依企劃 §5.2 實裝；取得途徑仍待版本擴充）

## M4 — 遺物與階段獎勵

- [x] 階段達標於**該回合 end_turn 流程**排入遺物二選一
- [x] 十一種遺物效果已接（含定額透支、餘料再利用、合成回手、緩衝日曆、灰燼稅）；**開局遺物二選一**（可略過）與里程碑同規則

## M5 — 打磨

- [x] 人類終端 UI、基本錯誤回傳、`README` 啟動說明、核心路徑 `pytest`
- [ ] 依企劃書 §5.2【6】～【9】擴充圖鑑實裝、平衡與測試
